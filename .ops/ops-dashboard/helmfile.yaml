environments:
    default:
        values:
            - ./env.yaml
---
repositories:
    - name: oauth2-proxy
      url: https://oauth2-proxy.github.io/manifests

releases:
    - name: ''
      chart: .
      values:
          - ./values.yaml.gotmpl
